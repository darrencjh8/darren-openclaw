[tool.pytest.ini_options]
asyncio_mode = "auto"
markers = [
    "live: marks tests that connect to live servers (IMAP/SMTP)",
    "integration: marks integration tests that require external services",
]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "W"]
