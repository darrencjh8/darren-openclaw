# Generated by ktmb-booking deploy.sh — idempotent, safe to re-run.
services:
  ktmb-booking:
    image: ktmb-booking:latest
    ports:
      - "127.0.0.1:8082:8082"
    volumes:
      - /tmp/ktmb_jobs.db:/tmp/ktmb_jobs.db
    env_file:
      - ~/openclaw-module-ktmb/.env
    environment:
      - KTMB_API_PORT=8082
    restart: unless-stopped
